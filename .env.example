NODE_ENV=development
PORT=3000

# Required locally and on Railway once PostgreSQL is attached.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chess_elite

# Required locally and on Railway. Generate a strong unique value per environment.
JWT_SECRET=replace-with-a-long-random-secret
